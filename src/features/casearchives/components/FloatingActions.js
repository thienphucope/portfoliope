import React from 'react';

/**
 * Floating actions toolbar component for the case vault.
 * Currently returns null as most actions are managed via FunctionBall.
 */

export default function FloatingActions() {
  return null;
}
